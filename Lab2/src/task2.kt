fun task2 (state: Boolean) {
    if (state) {

        //Номер задачі
        val tool = Tools()
        tool.printing(1, "Маючи список 10 GPS координат, визначити пару найближчих положень. (за формулою Гаверсинуса)")

        val list = mutableListOf<Double>()



    }
}